# Plan Directory

This directory is for actionable reboot / implementation planning documents.

Current docs:

- `../README_loco.md`
  - repo map, reboot context, current workflow summary
- `hpo_repo_reboot_plan.md`
  - concrete HPO-oriented repo organization plan, recovered run findings, and next implementation targets

Suggested use:

- keep `README_loco.md` as the broad local memory document
- keep `plan/` for time-bounded implementation plans, experiment roadmaps, and follow-up tasks
